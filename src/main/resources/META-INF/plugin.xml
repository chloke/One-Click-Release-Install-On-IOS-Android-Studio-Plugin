<idea-plugin>
    <id>io.github.chloke.oneclickreleaseinstallonios</id>
    <name>One Click Release Install on iOS</name>
    <vendor>chloke</vendor>

    <description><![CDATA[
        Adds a dedicated Android Studio toolbar button that builds, installs, and launches the
        current Flutter project on a physical iOS device in release mode, then automatically
        detaches from the Flutter process.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="One Click Release Install on iOS"
                           displayType="BALLOON"
                           isLogByDefault="true"/>
    </extensions>

    <actions>
        <action id="io.github.chloke.oneclickios.ReleaseInstallAction"
                class="io.github.chloke.oneclickios.OneClickReleaseAction"
                text="Install Flutter Release on iOS"
                description="Build, install, and launch this Flutter app in release mode on a physical iOS device"
                icon="/icons/releaseInstall.svg">
            <add-to-group group-id="MainToolbarRight"
                          anchor="before"
                          relative-to-action="SearchEverywhere"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
